# 人群计数损失函数设计（当前 v4 实现）

本文对应 `models/loss.py` 中的 `CrowdPointLoss`，不是早期含全局计数损失的版本。

## 1. 目的

ShanghaiTech 提供的是人头中心点，缺少可靠的边界框宽高。因此模型不采用标准 YOLO 的 IoU/DFL 框回归，而是将每个网格视为一个候选点：分类分支判断是否有人头，偏移分支细化人头中心。

## 2. 锚点与坐标

检测头使用 P2/4、P3/8、P4/16、P5/32 四层。每层锚点位于网格中心 `(grid_x + 0.5, grid_y + 0.5)`，并通过 stride 映射到输入图像坐标。

训练 v4 的输入为 1024×1024，共 87,040 个锚点：65,536（P2）+ 16,384（P3）+ 4,096（P4）+ 1,024（P5）。锚点总数会随 `imgsz` 改变。

每个 GT 中心点由 YOLO 虚拟框的 `x,y` 还原至输入图像坐标；虚拟框的 `w,h` 不参与此损失。

## 3. 正样本分配：Top-K + 自适应阈值

对单张图中的每个 GT 点：

1. 计算它与所有尺度锚点中心的欧氏距离；
2. 保留最近的 3 个候选锚点（`topk=3`）；
3. 对每个候选，只有当 `distance < 1.5 × stride` 时才保留为正样本；
4. 正样本分类目标为 1，偏移目标为：

$$
o_{target}=\frac{P_{gt}}{stride}-A
$$

其中 `A` 是特征图坐标系中的锚点中心。

该阈值会随尺度变化：P2 为 6 px、P3 为 12 px、P4 为 24 px、P5 为 48 px。相比旧版“最近一个锚点且固定 8 px 阈值”，它为每个 GT 提供多个学习位置，并避免高 stride 层几乎没有正样本。

### 锚点碰撞

不同 GT 可能选择同一个锚点。当前实现会累计并平均这些 GT 的偏移量，避免“最后写入者覆盖前者”的标签顺序问题；但该锚点只能预测一个分类分数和一个二维偏移，不能表达多个点。故极近的人头仍会形成冲突监督与潜在漏计，这是当前头结构的限制。

## 4. 总损失

$$
L = L_{cls} + 5L_{off}
$$

训练器需要三个槽位，因此返回值中的第三项是固定为 0 的占位项，并不参与反向传播。

### 4.1 分类损失

分类使用 `BCEWithLogitsLoss(reduction="none")` 后叠加 Focal 权重：

$$
w = y\alpha(1-p)^\gamma + (1-y)(1-\alpha)p^\gamma
$$

其中 `p=sigmoid(logit)`、`gamma=2.0`、`alpha=0.25`。加权 BCE 会对大量容易分类的背景降低权重。所有锚点上的损失求和后，以正锚点数归一化，再乘以 20。

### 4.2 偏移损失

仅在正锚点计算：

$$
L_{off}=mean(|o_{pred}-o_{target}|)
$$

最终权重为 5，以提高位置精度在总损失中的占比。

### 4.3 已移除的计数损失

早期版本曾使用全部锚点 `sigmoid` 概率的和逼近 GT 人数。当前版本已删除该项：训练早期数万个网格概率的累加会带来过强且不区分正负样本的梯度，干扰分类学习。任何旧文档中 `0.1 × L_count` 的公式均不再适用。

## 5. 与评估的关系

该损失优化的是点的分类和偏移，并不优化框 IoU。推理时为了兼容通用接口，点被包装为固定的 1×1 px 虚拟框；而标签虚拟框宽高是图像尺寸的 1%。两者的标准检测 IoU 近乎总是不达标，所以训练日志中的 Box P/R/mAP 不能用于评价该点模型。

请使用点级一对一匹配后的 Precision、Recall、F1 和图像级 MAE/RMSE。评测时必须固定置信度阈值、点匹配容忍距离、去重规则和 `max_det`，否则计数结果不可直接比较。
